# Utils Directory

This directory contains generic utility functions and shared helpers used across the application.

**Purpose:**
- Store cross-cutting concerns that don't belong to a specific domain (e.g., authentication helpers, password hashing).
- Provide database connection setup and session management (`get_db`).
- Store custom exception handlers, common string manipulations, or date/time formatters.
- House external service wrapper utilities (like basic email sending).
